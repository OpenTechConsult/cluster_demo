{
  "name": "cluster_demo",
  "version": "1.0.0",
  "description": "A demo Node.js application that scale using the cluster module. We are also going to dockerize the application",
  "main": "primary.js",
  "scripts": {
    "start": "node primary.js",
    "dev": "nodemon primary.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
